<h2><%= @task.title %></h2>
<p><%= @task.details %></p>
<%= check_box_tag "task[completed]", @task.id, @task.completed %>
<p>This task is not completed yet!</p>

<%= link_to "Edit the task", edit_task_path(@task) %>

<%= link_to "Back to list", "/tasks" %>
